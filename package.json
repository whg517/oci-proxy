{
  "name": "oci-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy for OCI container registries (Docker Hub, GHCR, GCR, k8s.io)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250610.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
